CASH Exchanged for returning users 

Set-CASMailbox -Identity "JForeman@mdot.state.md.us" -ActiveSyncEnabled $true `
    -MAPIEnabled $true `
    -EwsEnabled $true `
    -ImapEnabled $true `
    -PopEnabled $true `
    -OWAEnabled $true







